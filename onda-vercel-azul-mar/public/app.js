import {esc,money,icon,api,toast,img} from './shared.js';
let catalog={products:[],categories:[],settings:{}};
let bag=[];
try { const saved=JSON.parse(localStorage.getItem('onda.bag')||'[]'); if(Array.isArray(saved)) bag=saved.filter(i=>typeof i.variant_id==='string' && Number.isInteger(i.quantity) && i.quantity>0).slice(0,30); } catch{}
const main=document.getElementById('main');
const categoryName=id=>catalog.categories.find(c=>c.id===id)?.name||'Cole\u00e7\u00e3o';
const nav=(url)=>{history.pushState(null,'',url);render();window.scrollTo({top:0,behavior:'instant'});};
function saveBag(){localStorage.setItem('onda.bag',JSON.stringify(bag));chrome();}
function chrome(){
 const s=catalog.settings;
 document.getElementById('header').innerHTML=`<div class="announcement">${esc(s.announcement)}</div>${catalog.configured===false?'<div class="setup-banner" role="status">Pr&eacute;via da loja &middot; banco e painel aguardando configura&ccedil;&atilde;o do Supabase.</div>':''}<header class="header"><div class="header-inner container"><div><button class="icon-btn mobile-toggle" data-menu aria-label="Abrir menu" aria-expanded="false">${icon('menu')}</button><nav class="nav" aria-label="Principal"><a data-nav href="/colecao">Cole\u00e7\u00e3o</a><a data-nav href="/colecao?categoria=polos">Polos</a><a data-nav href="/colecao?categoria=camisas">Camisas</a><a data-nav href="/sobre">Sobre</a></nav></div><a class="brand" data-nav href="/" aria-label="${esc(s.name)}: in\u00edcio"><span class="wave">\ud83c\udf0a</span>${esc(s.name)}</a><div class="header-tools"><a data-nav class="icon-btn header-search" href="/colecao?busca=" aria-label="Buscar pe\u00e7as">${icon('search')}</a><a data-nav class="icon-btn" href="/sacola" aria-label="Abrir sacola">${icon('bag')}</a><span class="bag-count">(${bag.reduce((n,i)=>n+i.quantity,0)})</span></div></div></header>`;
 document.getElementById('footer').innerHTML=`<footer class="footer"><div class="container"><div class="footer-grid"><div class="footer-brand"><a data-nav class="brand" href="/"><span class="wave">\ud83c\udf0a</span>${esc(s.name)}</a><p>Uma curadoria independente de cl\u00e1ssicos.<br>Menos excessos. Mais personalidade.</p></div><div><h4>Explore</h4><a data-nav href="/colecao">Todas as pe\u00e7as</a><a data-nav href="/colecao?categoria=polos">Polos</a><a data-nav href="/colecao?categoria=camisas">Camisas</a><a data-nav href="/colecao?categoria=tricos">Tric\u00f4s</a></div><div><h4>Sobre a ${esc(s.name)}</h4><a data-nav href="/sobre">Nossa ess\u00eancia</a><a data-nav href="/contato">Fale com a loja</a>${s.instagram?`<a target="_blank" rel="noopener noreferrer" href="${esc(s.instagram)}">Instagram</a>`:''}</div><div><h4>Informa\u00e7\u00f5es</h4><a data-nav href="/politicas">Entrega e trocas</a><a data-nav href="/politicas">Privacidade</a><a data-nav href="/sacola">Sua sacola</a></div></div><div class="footer-bottom"><span>\u00a9 ${new Date().getFullYear()} ${esc(s.name)}. Loja independente de revenda. N\u00e3o afiliada \u00e0 Ralph Lauren.</span><a href="/admin">Acesso interno</a></div></div></footer>`;
}
function card(p){return `<article class="product-card"><a data-nav href="/produto/${esc(p.slug)}"><div class="photo">${p.images[0]?img(p.images[0],p.is_demo?'Imagem ilustrativa de roupa; n\u00e3o representa produto \u00e0 venda':p.name):''}<span class="photo-tag">${p.is_demo?'Ilustrativo':p.variants.every(v=>v.stock===0)?'Esgotado':`Sele\u00e7\u00e3o ${esc(catalog.settings.name)}`}</span><span class="card-plus">${icon('plus',16)}</span></div><div class="product-info"><span class="eyebrow">${esc(categoryName(p.category_id))} \u00b7 ${p.is_demo?'Exemplo':esc(p.brand)}</span><h3>${esc(p.name)}</h3><div class="price">${p.compare_cents>p.price_cents?`<span class="old-price">${money(p.compare_cents)}</span>`:''}${money(p.price_cents)}</div><p class="card-note">${p.is_demo?'Exemplo de cadastro \u00b7 imagem ilustrativa':[...new Set(p.variants.map(v=>v.color))].map(esc).join(' \u00b7 ')}</p></div></a></article>`;}
function home(){
 const s=catalog.settings, demo=catalog.products.some(p=>p.is_demo);
 const title=s.hero_title=== 'O essencial. Al\u00e9m do tempo.'?'O essencial.<br><em>Al\u00e9m do<br>tempo.</em>':esc(s.hero_title);
 const featured=catalog.products.filter(p=>p.featured).slice(0,4);
 main.innerHTML=`<section class="hero container"><div class="hero-grid"><div class="hero-copy"><span class="eyebrow">Curadoria independente</span><h1>${title}</h1><p>${esc(s.hero_text)}</p><a class="btn" data-nav href="/colecao">Explorar a cole\u00e7\u00e3o ${icon('arrow',18)}</a></div><div class="hero-visual"><img src="https://images.unsplash.com/photo-1617137968427-85924c800a22?auto=format&fit=crop&w=1300&q=90" alt="Editorial ilustrativo de moda cl\u00e1ssica masculina" fetchpriority="high"><div class="hero-stamp">${esc(s.name)}<br>CURADORIA</div><div class="hero-image-caption"><div><div class="eyebrow">A arte de vestir bem</div><strong>Naturalmente cl\u00e1ssico.</strong></div><span>01 / 06</span></div></div></div><div class="hero-bottom"><span>Estilo que permanece.</span><span>${demo?'Pr\u00e9via da loja \u00b7 produtos ilustrativos':'Pe\u00e7as selecionadas, uma a uma.'}</span></div></section><div class="container"><div class="brand-strip"><span>Cl\u00e1ssicos contempor\u00e2neos</span><i>\u2726</i><span>Eleg\u00e2ncia sem excessos</span><i>\u2726</i><span>Curadoria independente</span></div></div><section class="section container"><div class="section-head"><div><span class="eyebrow">O guarda-roupa essencial</span><h2>Pe\u00e7as que ficam.</h2></div><a class="text-link" data-nav href="/colecao">Ver toda a sele\u00e7\u00e3o ${icon('arrow',18)}</a></div><div class="product-grid">${(featured.length?featured:catalog.products.slice(0,4)).map(card).join('')}</div>${demo?'<p class="demo-note">Pr\u00e9via visual: fotos e pre\u00e7os ilustrativos. Cadastre suas pe\u00e7as reais no painel para come\u00e7ar.</p>':''}</section><section class="categories-band"><div class="container"><div class="section-head"><div><span class="eyebrow">Encontre o seu pr\u00f3ximo cl\u00e1ssico</span><h2>Escolhas com personalidade.</h2></div></div><div class="category-grid">${catalog.categories.slice(0,4).map(c=>{const p=catalog.products.find(p=>p.category_id===c.id);return `<a data-nav href="/colecao?categoria=${encodeURIComponent(c.id)}" class="category-tile">${p?.images[0]?img(p.images[0],`Imagem ilustrativa da categoria ${c.name}`):''}<div class="category-caption"><span>${esc(c.name)}</span>${icon('arrow',23)}</div></a>`;}).join('')}</div></div></section><section class="manifesto container"><div class="manifesto-image">${img('https://images.unsplash.com/photo-1441986300917-64674bd600d8?auto=format&fit=crop&w=1000&q=85','Editorial ilustrativo de uma boutique de roupas')}</div><div class="manifesto-copy"><span class="eyebrow">A ess\u00eancia ${esc(s.name)}</span><h2>O estilo n\u00e3o<br>precisa fazer<br><em>barulho.</em></h2><p>${esc(s.about)}</p><a class="text-link" data-nav href="/sobre">Conhe\u00e7a a nossa ess\u00eancia ${icon('arrow',18)}</a></div></section><div class="manifesto-quote">O tempo passa. O essencial permanece.</div>`;
}
function collection(){
 const params=new URLSearchParams(location.search);
 const sizes=[...new Set(catalog.products.flatMap(p=>p.variants.map(v=>v.size)))].sort();
 const colors=[...new Set(catalog.products.flatMap(p=>p.variants.map(v=>v.color)))].sort();
 const option=(value,label)=>`<option value="${esc(value)}">${esc(label)}</option>`;
 main.innerHTML=`<div class="container"><div class="page-title"><span class="eyebrow">A sele\u00e7\u00e3o completa</span><h1>Sua pr\u00f3xima escolha.</h1><p>Texturas, silhuetas e cores que atravessam esta\u00e7\u00f5es. Explore no seu tempo.</p></div><form id="filters" class="filters"><div class="field search-wide"><label for="search">Buscar pe\u00e7as</label><input id="search" name="search" type="search" placeholder="Polo, camisa, tric\u00f4..."></div><div class="field"><label for="category">Categoria</label><select name="category" id="category"><option value="">Todas</option>${catalog.categories.map(c=>option(c.id,c.name)).join('')}</select></div><div class="field"><label for="size">Tamanho</label><select name="size" id="size"><option value="">Todos</option>${sizes.map(s=>option(s,s)).join('')}</select></div><div class="field"><label for="color">Cor</label><select name="color" id="color"><option value="">Todas</option>${colors.map(s=>option(s,s)).join('')}</select></div><div class="field"><label for="max">Pre\u00e7o m\u00e1ximo</label><input name="max" id="max" inputmode="decimal" placeholder="R$"></div><div class="field"><label for="sort">Ordenar</label><select id="sort" name="sort"><option value="new">Mais recentes</option><option value="asc">Menor pre\u00e7o</option><option value="desc">Maior pre\u00e7o</option><option value="name">Nome A-Z</option></select></div><label class="check-field"><input type="checkbox" name="available"> Em estoque</label></form><section class="catalog-section"><div id="catalog-count" class="catalog-count"></div><div id="catalog-grid" class="product-grid"></div><div id="catalog-empty" hidden class="empty"><h2>Nenhuma pe\u00e7a por aqui.</h2><p>Tente outra busca ou remova os filtros.</p><button class="btn outline" id="clear-filters">Limpar filtros</button></div></section></div>`;
 const form=document.getElementById('filters'); form.elements.category.value=params.get('categoria')||''; form.elements.search.value=params.get('busca')||'';
 function filter(){
  const f=Object.fromEntries(new FormData(form)); const query=f.search.trim().toLocaleLowerCase('pt-BR'); const max=Number(f.max.replace(',','.'));
  const items=catalog.products.filter(p=>(!f.category||p.category_id===f.category)&&(!query||`${p.name} ${p.description} ${p.brand}`.toLocaleLowerCase('pt-BR').includes(query))&&p.variants.some(v=>(!f.size||v.size===f.size)&&(!f.color||v.color===f.color)&&(!f.available||v.stock>0))&&(!f.max||!Number.isFinite(max)||p.price_cents<=max*100));
  if(f.sort==='asc')items.sort((a,b)=>a.price_cents-b.price_cents);if(f.sort==='desc')items.sort((a,b)=>b.price_cents-a.price_cents);if(f.sort==='name')items.sort((a,b)=>a.name.localeCompare(b.name,'pt-BR'));
  document.getElementById('catalog-grid').innerHTML=items.map(card).join('');document.getElementById('catalog-count').textContent=`${items.length} pe\u00e7a${items.length===1?'':'s'} na sele\u00e7\u00e3o${items.some(p=>p.is_demo)?' \u00b7 exemplos ilustrativos':''}`;document.getElementById('catalog-empty').hidden=items.length>0;
 }
 form.addEventListener('input',filter);form.addEventListener('change',filter);form.addEventListener('submit',e=>e.preventDefault());document.getElementById('clear-filters').onclick=()=>{form.reset();filter();};filter();if(params.has('busca'))document.getElementById('search').focus();
}
function detail(slug){
 const p=catalog.products.find(p=>p.slug===slug); if(!p)return missing();
 document.title=`${p.name} | ${catalog.settings.name}`;
 const colors=[...new Set(p.variants.map(v=>v.color))];
 main.innerHTML=`<div class="container"><div class="breadcrumb"><a data-nav href="/colecao">Cole\u00e7\u00e3o</a> / ${esc(categoryName(p.category_id))} / ${esc(p.name)}</div><div class="detail-grid"><div><div class="detail-image">${p.images[0]?img(p.images[0],p.is_demo?'Foto ilustrativa':p.name,'id="main-photo"'):''}</div><div class="thumbnails">${p.images.map((url,i)=>`<button class="thumb" data-photo="${i}" aria-label="Ver foto ${i+1}">${img(url,`${p.name}: foto ${i+1}`)}</button>`).join('')}</div></div><div class="detail-copy"><span class="eyebrow">${esc(p.brand)} \u00b7 ${esc(categoryName(p.category_id))}</span><h1>${esc(p.name)}</h1><div class="detail-price">${p.compare_cents>p.price_cents?`<span class="old-price">${money(p.compare_cents)}</span>`:''}${money(p.price_cents)}</div><p>${esc(p.description)}</p>${p.is_demo?'<div class="notice">Exemplo de cadastro. Imagem e pre\u00e7o ilustrativos. Esta pe\u00e7a n\u00e3o est\u00e1 \u00e0 venda.</div>':''}<form id="add-form" class="detail-controls"><div class="field"><label for="product-color">Cor</label><select id="product-color" name="color" required><option value="">Selecione</option>${colors.map(c=>`<option>${esc(c)}</option>`).join('')}</select></div><div class="field"><label for="product-size">Tamanho</label><select id="product-size" name="variant_id" required disabled><option value="">Selecione a cor</option></select></div><div class="field"><label for="quantity">Quantidade</label><input id="quantity" name="quantity" type="number" value="1" min="1" max="1" required></div><p id="stock-info" class="muted"></p><button id="add-button" class="btn" ${p.is_demo?'disabled':''} type="submit">${p.is_demo?'Pe\u00e7a ilustrativa':'Adicionar \u00e0 sacola'} ${icon('bag',18)}</button></form><div class="details-stack"><details><summary>Composi\u00e7\u00e3o e cuidados</summary><p>${esc(p.composition||'Consulte a loja para mais informa\u00e7\u00f5es.')}</p></details><details><summary>Medidas da pe\u00e7a</summary><p>${esc(p.measurements||'Solicite as medidas \u00e0 loja antes de comprar.')}</p></details><details><summary>Entrega e trocas</summary><p>${esc(catalog.settings.shipping)}\n\n${esc(catalog.settings.returns)}</p></details></div></div></div></div>`;
 document.querySelectorAll('[data-photo]').forEach(b=>b.onclick=()=>document.getElementById('main-photo').src=p.images[Number(b.dataset.photo)]);
 const form=document.getElementById('add-form'); const size=form.elements.variant_id;
 form.elements.color.onchange=()=>{size.innerHTML='<option value="">Selecione o tamanho</option>'+p.variants.filter(v=>v.color===form.elements.color.value).map(v=>`<option value="${esc(v.id)}" ${v.stock===0&&!p.is_demo?'disabled':''}>${esc(v.size)}${v.stock===0?' \u00b7 indispon\u00edvel':''}</option>`).join('');size.disabled=!form.elements.color.value;document.getElementById('stock-info').textContent='';};
 size.onchange=()=>{const v=p.variants.find(v=>v.id===size.value);form.elements.quantity.max=v?.stock||1;form.elements.quantity.value=1;document.getElementById('stock-info').textContent=v?`${v.stock} unidade(s) dispon\u00edveis`:'';};
 form.onsubmit=async e=>{e.preventDefault();try{
  const v=p.variants.find(v=>v.id===size.value);const quantity=Number(form.elements.quantity.value);if(!v||p.is_demo||quantity<1||quantity>v.stock)throw new Error('Selecione uma varia\u00e7\u00e3o dispon\u00edvel.');
  const next=bag.map(i=>({...i}));const existing=next.find(i=>i.variant_id===v.id);if(existing)existing.quantity+=quantity;else next.push({variant_id:v.id,quantity});
  const button=document.getElementById('add-button');button.disabled=true;
  await api('/api/bag/validate',{method:'POST',body:JSON.stringify({items:next})});bag=next;saveBag();toast('Pe\u00e7a adicionada \u00e0 sacola.');
 }catch(error){toast(error.message,true);}finally{document.getElementById('add-button').disabled=!!p.is_demo;}};
}
function bagPage(){
 const lines=bag.map(i=>{const p=catalog.products.find(p=>p.variants.some(v=>v.id===i.variant_id));return {...i,p,v:p?.variants.find(v=>v.id===i.variant_id)};});
 main.innerHTML=`<div class="container"><div class="page-title"><span class="eyebrow">Sua sele\u00e7\u00e3o</span><h1>A sacola.</h1><p>Cada escolha, um novo cl\u00e1ssico.</p></div>${!lines.length?'<div class="empty"><h2>Um espa\u00e7o para boas escolhas.</h2><p>Sua sacola est\u00e1 vazia.</p><a data-nav class="btn" href="/colecao">Explorar a cole\u00e7\u00e3o</a></div><div class="section"></div>':`<div class="bag-grid"><div>${lines.map(i=>`<div class="bag-row">${i.p?.images[0]?img(i.p.images[0],i.p.name):'<span>Indispon\u00edvel</span>'}<div><h3>${esc(i.p?.name||'Pe\u00e7a indispon\u00edvel')}</h3><p>${esc(i.v?.color)} / ${esc(i.v?.size)}</p><label><span class="eyebrow">Qtd.</span> <input aria-label="Quantidade de ${esc(i.p?.name||'pe\u00e7a')}" type="number" min="1" max="${i.v?.stock||1}" value="${i.quantity}" data-quantity="${esc(i.variant_id)}"></label></div><div><div class="price">${money((i.p?.price_cents||0)*i.quantity)}</div><button class="link-button" data-remove="${esc(i.variant_id)}">Remover</button></div></div>`).join('')}</div><aside class="bag-summary"><h2>Resumo da sele\u00e7\u00e3o</h2><div class="total-line"><span>Subtotal</span><strong>${money(lines.reduce((n,i)=>n+(i.p?.price_cents||0)*i.quantity,0))}</strong></div><p>Frete e pagamento a confirmar. O subtotal ser\u00e1 revalidado no servidor antes do envio.</p>${catalog.settings.mode==='whatsapp'?'<button class="btn" id="request-order">Solicitar pelo WhatsApp</button>':'<button class="btn" disabled>Loja em prepara\u00e7\u00e3o</button>'}<p>Esta \u00e9 uma solicita\u00e7\u00e3o, n\u00e3o uma compra conclu\u00edda. N\u00e3o reserva estoque nem confirma pagamento.</p><div id="whatsapp-ready"></div></aside></div>`}</div>`;
 document.querySelectorAll('[data-remove]').forEach(b=>b.onclick=()=>{bag=bag.filter(i=>i.variant_id!==b.dataset.remove);saveBag();bagPage();});
 document.querySelectorAll('[data-quantity]').forEach(input=>input.onchange=()=>{const n=Number(input.value);const line=lines.find(i=>i.variant_id===input.dataset.quantity);if(!Number.isInteger(n)||n<1||n>(line.v?.stock||0)){toast('Quantidade acima do estoque dispon\u00edvel.',true);input.value=line.quantity;return;}bag.find(i=>i.variant_id===line.variant_id).quantity=n;saveBag();bagPage();});
 const button=document.getElementById('request-order');if(button)button.onclick=async()=>{button.disabled=true;try{const result=await api('/api/bag/validate',{method:'POST',body:JSON.stringify({items:bag})});if(!result.checkout_enabled)throw new Error('Pedidos ainda n\u00e3o foram ativados.');document.getElementById('whatsapp-ready').innerHTML=`<div class="notice">Subtotal revalidado: ${money(result.total_cents)}.</div><a class="btn" href="${esc(result.whatsapp_url)}" target="_blank" rel="noopener noreferrer">Abrir conversa no WhatsApp</a>`;button.hidden=true;}catch(error){toast(error.message,true);}finally{button.disabled=false;}};
}
function info(page){
 const s=catalog.settings;
 const content={sobre:`<span class="eyebrow">Nossa ess\u00eancia</span><h1>Menos excessos.<br>Mais significado.</h1><p>${esc(s.about)}</p><h2>Uma loja independente.</h2><p>A ${esc(s.name)} \u00e9 uma loja independente de revenda. N\u00e3o \u00e9 afiliada \u00e0 Ralph Lauren. As informa\u00e7\u00f5es de cada pe\u00e7a s\u00e3o fornecidas pela loja no cadastro do produto.</p>`,contato:`<span class="eyebrow">Vamos conversar</span><h1>Fale com a ${esc(s.name)}.</h1><p>Para consultar medidas, disponibilidade ou entrega, entre em contato.</p>${s.email?`<h2>E-mail</h2><a href="mailto:${esc(s.email)}">${esc(s.email)}</a>`:''}${s.whatsapp?`<h2>WhatsApp</h2><a href="https://wa.me/${esc(s.whatsapp)}" target="_blank" rel="noopener noreferrer">Conversar com a loja</a>`:''}${!s.email&&!s.whatsapp?'<div class="notice">Os canais de atendimento est\u00e3o em prepara\u00e7\u00e3o.</div>':''}`,politicas:`<span class="eyebrow">Informa\u00e7\u00f5es da loja</span><h1>Entrega e cuidado.</h1><div class="notice">Conte\u00fado inicial para revis\u00e3o do lojista antes de iniciar vendas. Esta p\u00e1gina n\u00e3o substitui uma pol\u00edtica completa e revisada.</div><h2>Entrega</h2><p>${esc(s.shipping)}</p><h2>Trocas e devolu\u00e7\u00f5es</h2><p>${esc(s.returns)}</p><h2>Privacidade</h2><p>A sacola \u00e9 salva neste navegador. O painel usa um cookie de sess\u00e3o para autenticar o administrador. Ao solicitar pelo WhatsApp, voc\u00ea sai deste site e compartilha o resumo com a loja naquele servi\u00e7o. Fotos ilustrativas e fontes podem ser carregadas de provedores externos. O lojista deve completar os dados do respons\u00e1vel, os canais de atendimento e sua pol\u00edtica de privacidade antes de publicar.</p>`};
 main.innerHTML=`<section class="content-page">${content[page]}</section>`;
}
function missing(){main.innerHTML='<section class="content-page"><span class="eyebrow">404</span><h1>Essa pe\u00e7a seguiu outro caminho.</h1><p>P\u00e1gina ou produto n\u00e3o encontrado.</p><div class="section"><a data-nav class="btn" href="/colecao">Voltar \u00e0 cole\u00e7\u00e3o</a></div></section>';}
function render(){
 document.title=`${catalog.settings.name} | O essencial. Al\u00e9m do tempo.`;
 const route=location.pathname.replace(/\/$/,'')||'/';chrome();
 if(route==='/')home();else if(route==='/colecao')collection();else if(route.startsWith('/produto/'))detail(decodeURIComponent(route.split('/')[2]));else if(['/sacola','/checkout'].includes(route))bagPage();else if(['/sobre','/contato','/politicas'].includes(route))info(route.slice(1));else missing();
}
document.addEventListener('click',e=>{const a=e.target.closest('a[data-nav]');if(a && !e.ctrlKey && !e.metaKey && !e.shiftKey){e.preventDefault();nav(a.getAttribute('href'));}const menu=e.target.closest('[data-menu]');if(menu){const open=document.querySelector('.nav').classList.toggle('open');menu.setAttribute('aria-expanded',String(open));}});
window.addEventListener('popstate',render);
window.addEventListener('storage',e=>{if(e.key==='onda.bag'){try{bag=JSON.parse(e.newValue||'[]');render();}catch{}}});
try {catalog=await api('/api/catalog');render();}catch(error){main.innerHTML=`<section class="content-page"><h1>A loja n\u00e3o carregou.</h1><p>${esc(error.message)} Confira a conex&atilde;o do Supabase e os passos do arquivo COMECE-AQUI.md. Seus dados n&atilde;o foram apagados.</p><a class="btn" href="/">Tentar novamente</a></section>`;}
